import { Controller, Get, NotFoundException, Param, Patch, Req } from '@nestjs/common'; import { PrismaService } from '../prisma/prisma.service';
@Controller('blocks') export class BlocksController { constructor(private prisma: PrismaService) {}
  @Get('neighborhood/:id') list(@Param('id') id:string) { return this.prisma.block.findMany({where:{neighborhoodId:id},orderBy:{number:'asc'},include:{completedBy:{select:{id:true,name:true}},neighborhood:{include:{city:true}}}}); }
  @Patch(':id/complete') async complete(@Param('id') id:string, @Req() req:{user:{sub:string}}) { const block=await this.prisma.block.findUnique({where:{id}}); if(!block) throw new NotFoundException('Quadra não encontrada.'); if(block.status==='COMPLETED') return block; const userId=req.user.sub; const updated=await this.prisma.block.update({where:{id},data:{status:'COMPLETED',completedAt:new Date(),completedById:userId}}); const cycle=await this.prisma.cycle.findFirst({where:{neighborhoodId:block.neighborhoodId},orderBy:{number:'desc'}}); if(cycle) await this.prisma.blockHistory.create({data:{action:'COMPLETED',blockId:id,cycleId:cycle.id,userId}}); await this.prisma.auditLog.create({data:{userId,action:'COMPLETE_BLOCK',description:`Quadra concluída: ${block.number}`}}); return updated; }
}
