'use client';
import { FormEvent, useState } from 'react'; import { ArrowRight, Building2, Lock, User } from 'lucide-react'; import { useRouter } from 'next/navigation';
const api=process.env.NEXT_PUBLIC_API_URL||'http://localhost:3001/api';
export default function Login(){const router=useRouter();const [username,setUsername]=useState('admin');const [password,setPassword]=useState('admin123');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const r=await fetch(`${api}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username,password})});const data=await r.json();if(!r.ok)throw new Error(data.message||'Não foi possível entrar.');localStorage.setItem('qc_token',data.accessToken);localStorage.setItem('qc_user',JSON.stringify(data.user));router.push('/dashboard');}catch(e){setError(e instanceof Error?e.message:'Erro inesperado.');}finally{setLoading(false)}}return <main className="login"><section className="login-brand"><div className="brand-mark"><Building2 size={30}/></div><h1>QuadraControl</h1><p>Controle simples, rápido e confiável para suas visitas.</p><div className="login-note">Acompanhe cada quadra, cada bairro e cada resultado em um só lugar.</div></section><section className="login-form"><form onSubmit={submit}><span className="eyebrow">BEM-VINDO DE VOLTA</span><h2>Entre na sua conta</h2><p>Use suas credenciais para acessar o painel.</p><label>Usuário<div className="input"><User size={18}/><input value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" required/></div></label><label>Senha<div className="input"><Lock size={18}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/></div></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={loading}>{loading?'Entrando…':<>Entrar <ArrowRight size={18}/></>}</button><small>Ambiente inicial: admin / admin123</small></form></section></main>}
